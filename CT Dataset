# Preprocessing the acquired CT Dataset
The first step will be to pre-process the CT images to extract the spinal cord region (thoracic) 
and normalize the images, removing noise and artifacts (e.g., tumors)
